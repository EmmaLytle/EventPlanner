@page
@model app.razor.WebApp.Pages.EventItem.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<div class="text-center mb-4">
    <h1 class="display-4">Event Items</h1>
</div>

<p>
    <a class="btn btn-primary btn-sm" asp-page="Create">+ New Event Item</a>
</p>
<table class="table table-hover">
    <thead>
        <tr>
            <th>
              Event
            </th>
           
            <th>
                @Html.DisplayNameFor(model => model.EventPlanItems[0].Description)
            </th>
            <th>
                Start Time
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.EventPlanItems)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDateTime)
            </td>
                        

            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
        }
    </tbody>
</table>
